/**
 * Hero imagery — default path consumed by `PMUHero` (`imageSrc`).
 *
 * MEDIA TODO — Replace current file at `/images/hero/image.png` with ONE of:
 * - Real ELEN healed powder brow close-up (consented), OR
 * - Elen mapping brows (process/educational), OR
 * - Elen working with client (consented, respectful crop)
 *
 * Avoid:
 * - Red lipstick / rose / fashion-campaign styling
 * - Stock model / generic beauty imagery
 * - Heavy glam makeup
 * - Any shot where brows are not the visual focus
 *
 * Ideal:
 * - Vertical-friendly portrait crop
 * - Neutral tones, natural skin texture
 * - Brow-forward composition
 * - Calm, premium studio feel (not loud editorial)
 *
 * Do not swap asset paths here until the real file is exported into `/public/images/hero/`.
 */
export const DEFAULT_PMU_HERO_IMAGE = "/images/hero/image.png" as const
